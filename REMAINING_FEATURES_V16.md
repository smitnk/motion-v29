# Remaining Features — V16

Completed in V16:
1. Multi-track audio composition/mixing during MP4 export.
2. Production-oriented rotoscope frame sampling/scaling pipeline.

Next two-feature block:
1. Interactive audio clip drag/trim handles in the timeline.
2. Reference-image pinch/rotate/opacity manipulation directly on canvas.

Build note: the repository still lacks `gradle-wrapper.jar`, so local wrapper compilation could not be performed in this environment. GitHub Actions should supply the configured Gradle environment.